// int abs(int n)
// ablolute value
// in c, only the int version exists
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int n, m;
	n = abs(23);
	m = abs(-11);

	printf("n = %d\n", n);
	printf("m = %d\n", m);

	return 0;
}
